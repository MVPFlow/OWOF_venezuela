{
  "name": "umaf-platform",
  "version": "0.1.0",
  "private": true,
  "description": "UMAF Social Platform - Monorepo",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "reset": "pnpm clean && powershell -Command \"Remove-Item -Recurse -Force apps/web/.next -ErrorAction SilentlyContinue; Write-Output 'Cache cleaned'\" && pnpm dev"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.1.0",
  "devDependencies": {
    "turbo": "^2.9.15"
  }
}
